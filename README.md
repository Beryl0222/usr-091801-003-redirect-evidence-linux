# 隐蔽导流证据协查

本项目用于内容安全团队汇集隐蔽导流线索。线索来源包括直播画面摘要、音频转写、正文、评论、昵称和收藏夹；原始私密素材由来源系统保管，本服务只接收定位信息、摘要和内容哈希。

案件状态为 `收集中`、`待初审`、`待复核`、`已冻结`、`已移送`、`申诉中`、`已纠正`。自动关联只形成建议，证据冻结与移送需要两名不同复核员确认。规则样例包含变体文字、拆分网址、短时二维码和站外风险链。

可用 `python3 service.py --check` 执行基础检查，或以 `python3 service.py --port 8000` 提供 `/health`。
